<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.byteforce_crew.educard">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.EduCard">
        <activity android:name="com.byteforce_crew.educard.boundaries.HomePage"
            android:theme="@style/Theme.EduCard.NoActionBar" />
        <activity android:name="com.byteforce_crew.educard.boundaries.RegistrationPage"/>
        <activity android:name="com.byteforce_crew.educard.boundaries.LoginPage">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>